# 常見問題

**無法載入主機？** 請用 http 啟動後端並先登入。

**自然語言無反應？** 檢查模型設定與 API 連線。

**集群群發（Fleet）在哪？** 說明目錄 **「Fleet（集群群發）」**。右側選 **🌐 Fleet**；預設報告口吻在右上角選單 **群發設定**。只會發給已開啟 Tab（或範圍內主機），並按 OS 分別產生命令。

**Python？** 需要 ≥ 3.10；正式安裝請用 `chibyterm.main:app`。
