# Natural language

After LLM is configured, describe intent in the right panel (e.g. “check disk usage”).

High-risk commands still require a confirmation card. Shell syntax is never translated when you switch UI language — only explanations and AI replies follow the selected language.
