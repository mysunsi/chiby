# 主机与终端

## 打开主机终端

1. 点击顶栏 **「+」**，在下拉中选择已添加的主机。  
2. 新 Tab 打开远程终端；右侧为 AI 面板。  
3. 状态栏可切换**目标操作系统**（影响自然语言→命令的适配）。

## 集群群发

跨机、按 OS 异构命令的群发与总体报告，见帮助目录 **「Fleet（集群群发）」**。

## 终端提示

- 键盘输入直通远程 Shell。  
- 状态栏字号与自动识别的目标 OS（只读）会影响自然语言命令适配；SSH 连接后会按远端探测校正。  
- Agent / Shell 模式仅作用于当前会话；跨机会话请用 Fleet。
